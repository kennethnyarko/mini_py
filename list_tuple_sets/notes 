Finding the index 
    In Python, “finding the index” usually means getting the position of an item inside a sequence like a list, string, or tuple.

       numbers = [10, 20, 30, 20]

       print(numbers.index(20))
       output =1

         Even though 20 appears twice, it returns the index of the first one.

     To check if something is in our list 
         print(50 in nubers )
         output=False 
         
         but if the number is there output will be true 

enumurate
for course in courses 
    print (course)
this will only print everything in courses 
   so to enumurate 
       for  index , course in enumurate (courses , start=1):
            print (index,courses )
            this starts numberig evry courses from 1  

list[] and tuples()
- if you need something that you can modify use lsit 
- something you can loop throughh use tuples we cant change or appened 

sets {}
- sets dont care about order 
- it get raid of duplicate 
- used to test a value is part of a set 
-use .intersection() to check if something repeats in two sets . eg print(courses.intersection(art_course))
-use to check if something is it set a but not in b use .difference()
-use union to join both a and b content 


# Mutable
list_1 = ['History', 'Math', 'Physics', 'CompSci']
list_2 = list_1

print(list_1)
print(list_2)

# list_1[0] = 'Art'

# print(list_1)
# print(list_2)


# Immutable
# tuple_1 = ('History', 'Math', 'Physics', 'CompSci')
# tuple_2 = tuple_1

# print(tuple_1)
# print(tuple_2)

# tuple_1[0] = 'Art'

# print(tuple_1)
# print(tuple_2)

# Sets
cs_courses = {'History', 'Math', 'Physics', 'CompSci'}

print(cs_courses)


# Empty Lists
empty_list = []
empty_list = list()

# Empty Tuples
empty_tuple = ()
empty_tuple = tuple()

# Empty Sets
empty_set = {} # This isn't right! It's a dict we cant use it 
use this rather empty_set = set()